name: retrofit_procatice
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  retrofit: any
  json_annotation:
  logger: any  #for logging purpose


dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: ^1.3.0
  build_runner: ^1.7.4
  json_serializable:



flutter:
  uses-material-design: true
